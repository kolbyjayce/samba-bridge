import { SMBOptions } from './types/SMBOptions';

export class SMBConnector {
    constructor(private options: SMBOptions) {
    
    }

    async connect(path: string): Promise<void> {
  
    }

  
}